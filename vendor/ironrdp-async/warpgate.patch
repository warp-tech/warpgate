diff -ru ironrdp-async-0.10.0/src/connector.rs base/src/connector.rs
--- ironrdp-async-0.10.0/src/connector.rs	2006-07-24 03:21:28.000000000 +0200
+++ base/src/connector.rs	2026-09-10 23:17:00.788913238 +0200
@@ -138,6 +138,7 @@
         server_name,
         server_public_key,
         kerberos_config,
+        connector.config.credssp_credentialless,
     )?;
 
     loop {
diff -ru ironrdp-async-0.10.0/src/framed.rs base/src/framed.rs
--- ironrdp-async-0.10.0/src/framed.rs	2006-07-24 03:21:28.000000000 +0200
+++ base/src/framed.rs	2026-09-14 17:28:48.132618129 +0200
@@ -170,6 +170,17 @@
         loop {
             match hint.find_size(self.peek()).map_err(io::Error::other)? {
                 Some((matched, length)) => {
+                    // An unmatched zero-length frame can never be skipped past: `read_exact(0)`
+                    // consumes nothing and performs no I/O, so looping on it would spin without
+                    // yielding or ever observing EOF. Any `PduHint` can report `(false, 0)` on a
+                    // malformed header (e.g. a fast-path byte with a zero length field), so this
+                    // is guarded here rather than in every hint.
+                    if length == 0 && !matched {
+                        return Err(io::Error::new(
+                            io::ErrorKind::InvalidData,
+                            "PduHint reported a zero-length unmatched PDU; cannot make progress",
+                        ));
+                    }
                     let bytes = self.read_exact(length).await?.freeze();
                     if matched {
                         return Ok(bytes);
